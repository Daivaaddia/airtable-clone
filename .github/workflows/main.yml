name: Run Scrapers

on:
  workflow_dispatch:
  schedule:
    - cron: "56 * * * *"

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        run: echo "Hello"
